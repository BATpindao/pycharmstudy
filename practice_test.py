"""
冒泡排序🫧
"""
nums = [22,55,3,88,12,22,-1,0,-5]

for i in range(0,len(nums)-1):
    for j in range(0,len(nums)-i-1):
        if nums[j] > nums[j+1]:
            nums[j],nums[j+1] = nums[j+1], nums[j]
    print(f'第{i+1}轮比较：{nums},比较的列表长度：{len(nums)-i-1}')



print()
def insertion_sort_with_process(arr):
    for i in range(1, len(arr)):  # 从第2个元素开始（索引1）
        key = arr[i]  # 当前要插入的元素（key）
        j = i - 1  # 已排序部分的最后一个索引

        print(f"\n第{i}轮：取出 key = {key}")  # 本轮开始提示

        # 向左比较，找到插入位置，同时右移更大元素
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]  # 把较大元素右移一位
            print(f'j的变化- {j}', arr)
            j -= 1  # 继续向左比较


        arr[j + 1] = key  # 把 key 插入到正确位置

        # 每轮结束后打印当前列表状态
        print(f"  第{i}轮结束后：列表 = {arr}")

    # 排序完成后打印最终结果和统计信息
    print(f"\n最终排序结果： {arr}")
    return arr  # 返回已排序的列表（原地修改）

# 测试数据
nums = [3,8,4,2]
insertion_sort_with_process(nums)